# GitHub OAuth App credentials
# These values are embedded in the binary at compile time.
# Security Note: For desktop apps using GitHub OAuth App (not GitHub App),
# client secret exposure is acceptable as OAuth flow still requires user consent.
GITHUB_CLIENT_ID=your_client_id_here
GITHUB_CLIENT_SECRET=your_client_secret_here
